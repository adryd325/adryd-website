---
import Logo from "./Logo.svg";

const today = new Date();

let ornamentsEmoji = "💫 ⭐ 🌟 ✨ 🌙 🐈‍⬛".split(" ");
if (process.env.NODE_ENV === "development") {
  ornamentsEmoji = "🚧 🏗️ 🔨 🧰 ⚠️ 🦺 🏭 🚛 📏".split(" ");
}
const ornaments = [];
let index;
for (let i = 0; i < 4; i++) {
  index = Math.floor(Math.random() * ornamentsEmoji.length);
  ornaments.push(ornamentsEmoji[index]);
  ornamentsEmoji.splice(index, 1);
}
---

<style lang="scss">
  @use "@/lib" as *;

  .footer {
    padding: rem(1.5) rem(0.5);
    background-color: $color-stone-100;
    color: $color-stone-700;
    margin-top: rem(3);
    font-size: $text-xs;
    line-height: 1.5;
    @include large() {
      font-size: $text-sm;
    }
    @include dark() {
      background-color: $color-gray-800;
      color: $color-gray-200;
    }
  }

  a {
    color: inherit;
  }

  .logo {
    display: block;
    color: $color-stone-800;
    text-decoration: none;
    @include dark() {
      color: $color-gray-300;
    }
    svg {
      height: 68px;
      // specify width for old browsers
      width: 61.52px;
      @include large() {
        width: 66.94px;
        height: 74px;
      }
    }
  }
</style>

<div class="footer" role="contentinfo">
  <div class="container">
    <div>
      <a class="logo" href="/">
        <!--[if lt IE 9]> <img src="/static/compat/Logo.png" height="68px" width="62px"> <![endif]-->
        <!--[if gt IE 8]><!-->
        <Logo />
        <!--<![endif]--></a
      >
    </div>
    <span
      >copyright &copy; {today.getFullYear()} Ariana "adryd" redacted. this website
      is dual licensed under the MIT license and <a
        href="http://creativecommons.org/licenses/by/4.0/">CC BY 4.0</a
      > license. not all images, vector graphics, scripts and other assets fall under
      this license. visit <a href="/licenses/">licenses</a> for detailed copyright
      information. this footer is mostly here because it looks cool.
      <b
        >appreciate my work? <a href="//github.com/sponsors/adryd325"
          >consider donating.</a
        ></b
      >
      <br />
      <span>{ornaments.join(" ")}</span>
    </span>
  </div>
</div>
